import 'package:flutter/material.dart';

class GoEmpty extends StatelessWidget {
  const  GoEmpty({super.key});
  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text("Simelleee",style: TextStyle(fontWeight: FontWeight.bold),)
    );
  }
}